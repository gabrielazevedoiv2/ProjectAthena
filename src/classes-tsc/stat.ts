export default class Stat {
    baseVal: number;
    bonusVal: number;
    constructor(name:string, baseVal: number, bonusVal: number) {
        this.baseVal = baseVal;
        this.bonusVal = bonusVal;
    }
}